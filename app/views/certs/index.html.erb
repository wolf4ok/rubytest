<section id="scroll">
  <div>
    <nav class="nav justify-content-end">
      <nav class="nav flex-column">
        <%= link_to 'Користувачі', users_path, class: "btn btn-dark" %>
        <%= link_to 'Сертифікати', certificates_path, class: "btn btn-dark" %>
        <%= link_to 'Навички', skills_path, class: "btn btn-dark" %>
        <%= link_to 'Типи сертифікатів', certs_path, class: "btn btn-dark" %>
        <a href=""> Акаунти</a>
      </nav>
    </nav>
  <div class="container px-5">
    <div class="row gx-5 align-items-center">
      <div class="col-lg-6 order-lg-1">
        <div class="p-5">
          <h2> Certs </h2>
        </div>
      </div>
    </div>
    <div style ="width: 60%; margin: 0 auto;">
      <div>
        <form class="d-flex">
          <input class="form-control me-2" name="s" type="search" placeholder="Пошук за назвою" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Знайти</button>
        </form>
      </div>
    </div>
  </div>
    <div class="d-flex">
      <%= button_to 'Усі типи сертифікатів', certs_path, method: :get %>
      <%= button_to 'Новий тип сертифікату', new_cert_path, method: :get %>
    </div>
  <div>
    <% unless @certs.empty? %>
      <table>
        <tr><th>Тип сертифікату</th><th>Описання</th></tr>
        <% @certs.each do |cert| %>
          <tr><td><%= link_to cert.name, cert_path(cert) %></td><td><%=  cert.description %></td></tr>
          <tr><td align="left">
            <%= button_to 'Змінити', edit_cert_path(cert), method: :get %>
          </td><td align="right">
            <%= button_to 'Видалити', cert_path(cert), method: :delete %>
          </td></tr>
        <% end %>
      </table>
    <% else %>
      <h2> No Certs </h2>
    <% end %>
  </div>
  </div>
</section>